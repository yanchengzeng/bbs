# Backend environment example
#
# Copy to backend/.env and edit as needed:
#   cp env.example .env
#
# DB:
# - Default (SQLite): leave DATABASE_URL unset (FastAPI will use sqlite:///./bbs.db)
# - PostgreSQL: set DATABASE_URL and install postgres deps (requirements-postgres.txt)
#
# DATABASE_URL=postgresql://user:password@localhost:5432/bbs

# JWT
SECRET_KEY=dev-secret-key-for-local-testing
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS
FRONTEND_URL=http://localhost:5173

# Google OAuth (optional; app still works for anonymous posting without these)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

